#include "game.h"

int main()
{
    Game gameObject(10);    // Create an object from the Game -class and give 10 as a parameter for the constructor

    gameObject.play();      // Call play() -method

    return 0;
}
